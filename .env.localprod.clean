# Local production-like environment (CLEAN STATE) for Docker Compose

# Django Core Settings
DJANGO_SECRET_KEY=localprod-clean-secret-key-not-for-production
DEBUG=False
ALLOWED_HOSTS=localhost,127.0.0.1,0.0.0.0
CSRF_TRUSTED_ORIGINS=http://localhost:8001

# Database Configuration  
DATABASE_NAME=kni_webapp_localprod_clean
DATABASE_USER=kni_user
DATABASE_PASSWORD=localprod_db_password

# Redis Configuration
REDIS_PASSWORD=localprod_redis_password

# Application Configuration
WAGTAILADMIN_BASE_URL=http://localhost:8001
SECURE_SSL_REDIRECT=False

# Tenant seeding (enabled for clean state)
SEED_TENANT_DATA=true
TENANT_SCHEMA=johann
TENANT_HOSTNAME=johann.localhost
TENANT_PORT=8001
TENANT_ADMIN_USER=JCleemannByg
TENANT_ADMIN_PASSWORD=admin123
TENANT_ADMIN_EMAIL=admin@jcleemann.test

# Optional admin user for first boot
DJANGO_SUPERUSER_EMAIL=admin@local.test
DJANGO_SUPERUSER_PASSWORD=admin123

# No backup data loading
LOAD_BACKUP_DATA=