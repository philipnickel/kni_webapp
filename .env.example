# KNI Webapp Environment Configuration
# Copy this to .env if you need to override Docker Compose defaults

# ==============================================================================
# Docker Local Development (docker-compose.local.yml)
# ==============================================================================
# All settings below are optional - Docker Compose provides sensible defaults
# Only uncomment if you need to override the defaults

# Django Core Settings
# DJANGO_SECRET_KEY=your-secret-key-here
# DEBUG=False
# ALLOWED_HOSTS=localhost,127.0.0.1,your-domain.com
# CSRF_TRUSTED_ORIGINS=http://localhost:8001

# Database Configuration  
# DATABASE_NAME=kni_webapp
# DATABASE_USER=kni_user
# DATABASE_PASSWORD=your_secure_password

# Redis Configuration
# REDIS_PASSWORD=your_redis_password

# Application Configuration
# WAGTAILADMIN_BASE_URL=http://localhost:8001
# SECURE_SSL_REDIRECT=False

# Baseline Data Loading
# LOAD_BASELINE=true
# SEED_TENANT_DATA=false

# ==============================================================================
# Production Environment (.env.production template)
# ==============================================================================
# For production deployment, copy .env.production to .env and customize:
# - Set secure passwords and secret keys
# - Configure your domain names
# - Enable SSL redirect
# - Set up database and Redis URLs